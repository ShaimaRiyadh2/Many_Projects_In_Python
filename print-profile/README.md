# print-profile

مشروع بايثون خفيف يطبع الاسم والعمر والمدينة بطرق وأنواع مختلفة.

## التشغيل

```bash
python main.py
```

## التعديل

غيّر القيم في أعلى `main.py`:

- `name` — نص (`str`)
- `age` — عدد صحيح (`int`)
- `city` — نص (`str`)
